<div class="hambuger-menu-wrapper">
    <input type="checkbox" class="hambuger-menu-checkbox">
    <label class="hambuger-menu-icon">
        <span></span>
        <span></span>
        <span></span>
    </label>
    <div class="menu-content">
        <!--  ADD MENU POINTS HERE   -->
        <ul>
            <li>Skills</li>
            <li>About Me</li>
            <li>Projekte</li>
            <li>Kontakt</li>
        </ul>


    </div>
</div>

<style lang="scss">
    .hambuger-menu-wrapper{

        --menu-icon-color:rgb(0, 0, 0);
        --menu-background-color:rgb(16, 16, 16);
        --font-size:1.25rem;
        --font-color:rgb(255, 255, 255);
        --hover-font-color:#e44064;
        --text-transform: uppercase;
        --hover-transformY:-2px;
        --hover-transformX:4px;
        --menu-max-width:1920px;
        --open-menu-opacity:0.95;
        --menu-active-icon-color:rgb(255, 255, 255);

        position: relative;
        z-index: 999;
        align-items: center;
        height: 100%;
        aspect-ratio: 1 / 1;
        max-width: 64px;

    }


    .hambuger-menu-checkbox{
        position: absolute;
        z-index: 3;
        width: 100%;
        height: 100%;
        opacity: 0;
        cursor: pointer;

          &:checked + .hambuger-menu-icon {

            span{
                background-color:var(--menu-active-icon-color);
            }

            span:nth-child(1) { 
                transform: rotate(45deg) translate(12%,12%); 
                margin: 0px;
            }
            span:nth-child(2) { 
                opacity: 0; 
                margin: 0px;
            }
            span:nth-child(3) { 
                margin: 0px;
                transform: rotate(-45deg) translate(12%,12%); 
            }
        }

            &:checked ~ .menu-content{
                width: 100vw;
                opacity: var(--open-menu-opacity);
            }
    }

    .menu-content{
        position: fixed;
        z-index: 1;
        top: 0px;
        left: 0px;
        height: 100vh;
        width: 100px;
        background-color: var(--menu-background-color);
        opacity: 0;
        transition: width 0.25s ease-in-out, opacity 0.20s ease-in-out;
        display: flex;
        justify-content: center;

        ul{
            width: 100%;
            max-width: var(--menu-max-width);
            padding-inline-start: 0px;
            margin-block-start: 0px;
            margin-block-end: 0px;
            padding-left: 2rem;
            box-sizing: border-box;
            list-style: none;
            position: absolute;
            top: 50%;
            transform: translate(0%,-50%);
            font-size: var(--font-size);
            color: var(--font-color);

            li{
                width: max-content;
                margin: 0.5rem 0px;
                text-transform: var(--text-transform);
                transition: color 0.25s ease-in-out, transform 0.125s ease-in-out;
                &:hover{
                    cursor: pointer;
                    color: var(--hover-font-color);
                    transform: translate(var(--hover-transformX),var(--hover-transformY));
                }
            }
        }

    }

    .hambuger-menu-icon{
        position: absolute;
        z-index: 2;
        top: 0px;
        left: 0px;
        width: 100%;
        height: 100%;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;

        span{
            margin: 5px;
            display: block;
            width: 34px;
            background-color: var(--menu-icon-color);
            height: 3px;
            transition: transform 0.25s ease-in-out, opacity 0.25s ease-in-out, margin 0.25s ease-in-out;
        }
    }

</style>